# Hoist dependencies into a flat node_modules so Expo / Metro can resolve
# packages/shared and packages/mock-ai from the monorepo root (PRD §14 Step 1).
node-linker=hoisted
# RN / Expo pull in many transitive peer ranges; don't fail install on peer warnings.
strict-peer-dependencies=false
