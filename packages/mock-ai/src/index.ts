// Entry point for @ai-agent-desk/mock-ai.
//
// Holds the Mock LLM Adapter (pure TypeScript, no real LLM — PRD §3.5 / §11):
// it turns a customer message into a structured, Zod-validated AIAnalysis.
//
// Scaffold placeholder for Step 1 — the adapter (validated against
// AIAnalysisSchema, with a fallback path) arrives in Step 4 (§11).
export const MOCK_AI_PACKAGE_NAME = '@ai-agent-desk/mock-ai';
