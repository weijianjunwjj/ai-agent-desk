# apps/server environment (copy to .env; .env is gitignored).
PORT=3001
# SQLite path is resolved relative to the schema dir (apps/server/prisma/).
DATABASE_URL="file:./dev.db"

# v0.2 M2 (LLM Gateway) will add:
# LLM_PROVIDER=mock      # mock | real
# LLM_API_KEY=...        # never commit a real key
