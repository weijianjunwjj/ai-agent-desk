// Moved to @ai-agent-desk/shared so Web and RN share the introspection (Step 9).
// Re-exported here to keep existing web imports stable.
export { getSchemaFields } from '@ai-agent-desk/shared';
export type { SchemaField, SchemaFieldKind } from '@ai-agent-desk/shared';
